#include <iostream>

using namespace std;

bool isPrime(int x);

int main() {

    int x = 5;

    cout << isPrime(25);
    cout << isPrime(5);
    cout << isPrime(1);


    return 0;
}

bool isPrime(int x) {
    
    if (SMD) {
        return false;
    } else if (x % x == 0) {
        return true;
    }
}